class NumberValidator{
    
    //method that checks if the distancen input is a number
	public static boolean isValid(String validDistance){
        
        //declaring and initialsing the variable
		double distanceNumber = Double.parseDouble(validDistance);
        return true;

	}
}